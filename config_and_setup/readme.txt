Files in this folder:

*_Backup_yymmdd.sql = a backup of the db_test database tables as of that date. These files are used to restore the database to its condition as of that date

*DataInitialization_yymmdd.sql = is just the table definitions and some sample data. These files are used to create an empty configuration with just the starter records.